<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}THIS BURGER GOOD{{else}}GOBBLE GOBBLE{{/if}}
	</button>


	<button class="delete-burger" data-id="{{this.id}}">MEH!</button>

</li>